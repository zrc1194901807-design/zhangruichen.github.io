# Static site package

This package contains the published site's HTML, CSS, JavaScript, images, and video assets.

## Local preview

Run a static HTTP server in this directory, for example:

`powershell
py -m http.server 8080
`

Then open http://localhost:8080 . Do not double-click index.html, because browsers may restrict module scripts or root-relative resources under file:// URLs.
